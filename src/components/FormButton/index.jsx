import React from 'react';

import Button from '../Button';

const FormButton = () => (
  <div className="form__item">
    <Button className="form__input button button--active" text="Search" isSubmit />
  </div>
);

export default FormButton;
